apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: task1
spec:
  steps:
    - name: task-one-step-one
      image: centos
      command: ["bash", "-c"]
      args:
        - echo "=======================";
          echo "Test npm install 10.9.0";
          echo "=======================";
          npm install -g npm@10.9.0 && echo "Done";
          echo "=======================";
          echo "Its over"
          echo "=======================";
